---
description: Show where the current Formento project stands in the phase pipeline
---

Read the project's Spec IR and docs to determine: current phase, entry mode used, migration vs. fresh-rebuild (if derived), and which checkpoints are approved vs. pending. Summarize in a short status line plus a one-line note on what the next action is (which `/phaseN-*` command to run next, or what decision is blocking progress).

